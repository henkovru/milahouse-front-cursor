# Инструкция по интеграции фронтенда MilaHouse с Битриксом

## 📋 Обзор проекта

Проект представляет собой статический фронтенд для сайта гостевого дома MilaHouse, который необходимо интегрировать с CMS Битрикс.

### Технологический стек:
- **Сборка**: Webpack 5
- **Препроцессор**: SCSS
- **Фреймворки**: Tailwind CSS
- **Библиотеки**: 
  - Swiper (слайдеры)
  - Air Datepicker (календари)
  - Fancybox (модальные окна)

---

## 📁 Структура проекта

```
src/
├── includes/          # Переиспользуемые компоненты
│   ├── header.html    # Шапка сайта (меню, контакты)
│   └── footer.html    # Подвал (контакты, футер)
├── dialogs/           # Модальные окна
│   ├── booking-modal.html
│   ├── room-modal.html
│   ├── lead-modal.html
│   ├── success-modal.html
│   ├── error-modal.html
│   └── consent-modal.html
├── scss/              # Стили (SCSS)
│   ├── main.scss      # Главный файл импортов
│   └── ui-kit/        # Компоненты стилей
├── js/                # JavaScript
│   └── main.js        # Главный файл логики
├── img/               # Изображения
│   ├── png/           # Растровые изображения
│   └── svg/           # Векторные иконки
├── fonts/             # Шрифты
└── *.html             # Основные страницы
    ├── index.html      # Главная страница
    ├── admin.html      # Админ панель
    ├── admin-login.html # Страница входа в админку
    ├── 404.html        # Страница 404
    └── privacy.html    # Политика конфиденциальности
```

---

## 🔧 Интеграция с Битриксом

### 1. Подготовка файлов для Битрикса

#### 1.1. Сборка проекта

```bash
npm install
npm run build
```

После сборки все файлы будут в папке `dist/`:
- `dist/css/main.css` - скомпилированные стили
- `dist/js/main.js` - скомпилированный JavaScript
- `dist/img/` - изображения
- `dist/fonts/` - шрифты
- `dist/*.html` - HTML страницы

#### 1.2. Структура для Битрикса

Рекомендуемая структура в шаблоне Битрикса:

```
/local/templates/[your_template]/
├── header.php          # Шапка (на основе src/includes/header.html)
├── footer.php          # Подвал (на основе src/includes/footer.html)
├── css/
│   └── main.css        # Из dist/css/main.css
├── js/
│   └── main.js         # Из dist/js/main.js
├── img/                 # Из dist/img/
└── fonts/               # Из dist/fonts/
```

---

## 🔌 Интеграция компонентов

### 2.1. Header (Шапка сайта)

**Файл**: `src/includes/header.html`

**Что нужно интегрировать:**
- Меню навигации
- Контактная информация (адрес, телефон)
- Мессенджеры (Telegram, VK, WhatsApp)
- Кнопка "Выбрать номер"

**Для Битрикса:**
1. Скопируйте содержимое `header.html` в `header.php`
2. Замените статические данные на компоненты Битрикса:
   ```php
   // Вместо: Геленджик, ул.Светлая, 25
   <?= $arResult['ADDRESS'] ?>
   
   // Вместо: 8 918 132 95 75
   <?= $arResult['PHONE'] ?>
   ```
3. Меню можно интегрировать через компонент `bitrix:menu`

### 2.2. Footer (Подвал)

**Файл**: `src/includes/footer.html`

**Что нужно интегрировать:**
- Секция контактов (3 блока: О нас, Горячая линия, Контакты)
- Футер с копирайтом

**Для Битрикса:**
1. Скопируйте в `footer.php`
2. Замените статические данные на данные из Битрикса
3. Кнопка "Подробнее" ведет на `#about` (якорная ссылка)

### 2.3. Модальные окна

**Файлы**: `src/dialogs/*.html`

**Важно**: Модальные окна используют библиотеку Fancybox. Они загружаются динамически через атрибут `data-fancybox-dialog`.

**Для Битрикса:**
1. Скопируйте HTML модальных окон в отдельные файлы или компоненты
2. Убедитесь, что Fancybox подключен
3. Модальные окна:
   - `booking-modal.html` - форма бронирования
   - `room-modal.html` - детали номера с календарем
   - `lead-modal.html` - форма лида
   - `success-modal.html` - успешная отправка
   - `error-modal.html` - ошибка
   - `consent-modal.html` - согласие на обработку данных

---

## 📊 Работа с формами

### 3.1. Форма бронирования (Hero форма)

**Файл**: `src/index.html`, форма в блоке Hero

**Важно**: Формы отправляются стандартным способом через HTML `<form>` submit. JavaScript выполняет только валидацию.

**Поля формы:**
- Дата заезда (checkin)
- Дата отъезда (checkout)
- Количество взрослых (adults)
- Количество детей (children)

**Для Битрикса:**
- Создайте компонент формы или используйте стандартный компонент Битрикса
- Настройте обработчик формы на отправку данных через стандартный механизм Битрикса
- JavaScript выполнит валидацию перед отправкой

### 3.2. Форма бронирования номера

**Файл**: `src/dialogs/booking-modal.html`

**Поля формы:**
- Имя (name)
- Телефон (phone)
- Дата заезда (checkin)
- Дата отъезда (checkout)
- Количество взрослых (adults)
- Количество детей (children)
- Комментарий (comment, опционально)

**Для Битрикса:**
- Создайте веб-форму или используйте компонент формы Битрикса
- Настройте обработчик на сохранение в инфоблок/базу данных
- Форма отправляется стандартным способом через HTML form submit

### 3.3. Получение данных о бронированиях для календарей

**ВАЖНО**: Календари автоматически блокируют забронированные даты на основе данных из `data-атрибутов`.

#### Формат данных

Календари читают данные из атрибутов `data-bookings` или `data-room-bookings`:

```html
<!-- Для конкретного номера -->
<div class="rooms__card" 
     data-room-id="1" 
     data-room-bookings='[{"checkin":"2026-02-15","checkout":"2026-02-20","roomId":"1"}]'>
</div>

<!-- Для всех бронирований (админ панель) -->
<div data-bookings='[{"checkin":"2026-02-15","checkout":"2026-02-20","roomId":"1"},{"checkin":"2026-03-01","checkout":"2026-03-05","roomId":"2"}]'>
</div>
```

#### Структура объекта бронирования

Каждый объект в массиве должен содержать:
- `checkin` (string, обязательное) - Дата заезда в формате `YYYY-MM-DD`
- `checkout` (string, обязательное) - Дата отъезда в формате `YYYY-MM-DD`
- `roomId` (string, обязательное) - ID номера (например, `"1"`, `"2"`, `"3"`)

**Важно**: Даты `checkin` и `checkout` блокируются в календаре (нельзя выбрать эти даты).

#### Примеры данных

**Пример 1: Одно бронирование**
```json
[
  {
    "checkin": "2026-02-15",
    "checkout": "2026-02-20",
    "roomId": "1"
  }
]
```

**Пример 2: Несколько бронирований для разных номеров**
```json
[
  {
    "checkin": "2026-02-15",
    "checkout": "2026-02-20",
    "roomId": "1"
  },
  {
    "checkin": "2026-02-25",
    "checkout": "2026-03-01",
    "roomId": "1"
  },
  {
    "checkin": "2026-03-10",
    "checkout": "2026-03-15",
    "roomId": "2"
  }
]
```

**Пример 3: Пустой массив (нет бронирований)**
```json
[]
```

#### Интеграция в Битрикс

**Шаг 1**: В компоненте вывода номеров добавьте атрибут `data-room-bookings`:

```php
<?php
// Получаем бронирования для конкретного номера из инфоблока
$roomId = $arItem['ID']; // ID номера
$bookings = []; // Получите из БД/инфоблока

// Формируем массив для календаря
$bookingsForCalendar = [];
foreach ($bookings as $booking) {
    // Проверяем, что бронирование относится к этому номеру
    if ($booking['ROOM_ID'] == $roomId) {
        $bookingsForCalendar[] = [
            'checkin' => date('Y-m-d', strtotime($booking['CHECKIN'])),
            'checkout' => date('Y-m-d', strtotime($booking['CHECKOUT'])),
            'roomId' => (string)$roomId
        ];
    }
}

$bookingsJson = json_encode($bookingsForCalendar, JSON_UNESCAPED_UNICODE);
?>
<div class="rooms__card" 
     data-room-id="<?= $roomId ?>"
     data-room-bookings='<?= htmlspecialchars($bookingsJson, ENT_QUOTES, 'UTF-8') ?>'>
    <!-- Содержимое карточки номера -->
</div>
```

**Шаг 2**: Для админ панели передайте все бронирования:

```php
<?php
// Получаем все бронирования из инфоблока
$allBookings = []; // Получите из БД/инфоблока

$bookingsForCalendar = [];
foreach ($allBookings as $booking) {
    $bookingsForCalendar[] = [
        'checkin' => date('Y-m-d', strtotime($booking['CHECKIN'])),
        'checkout' => date('Y-m-d', strtotime($booking['CHECKOUT'])),
        'roomId' => (string)$booking['ROOM_ID']
    ];
}

$bookingsJson = json_encode($bookingsForCalendar, JSON_UNESCAPED_UNICODE);
?>
<form id="adminForm" data-bookings='<?= htmlspecialchars($bookingsJson, ENT_QUOTES, 'UTF-8') ?>'>
    <!-- Форма админ панели -->
</form>
```

#### Важные замечания

1. **Формат дат**: Обязательно используйте формат `YYYY-MM-DD` (например, `2026-02-15`)
2. **Экранирование**: Используйте `htmlspecialchars()` для безопасного вывода JSON в HTML
3. **Кодировка**: Убедитесь, что JSON кодируется в UTF-8
4. **Пустые данные**: Если бронирований нет, передайте пустой массив `[]`
5. **roomId как строка**: Значение `roomId` должно быть строкой (например, `"1"`, а не `1`)

---

## 🎨 Стили и компоненты

### 4.1. Подключение стилей

**Файл**: `src/scss/main.scss` - главный файл импортов

**Для Битрикса:**
1. Скопируйте `dist/css/main.css` в шаблон
2. Подключите в `header.php`:
   ```php
   <link rel="stylesheet" href="<?= SITE_TEMPLATE_PATH ?>/css/main.css">
   ```

### 4.2. CSS Переменные

**Файл**: `src/css/variables.css`

**Основные переменные:**
- Цвета: `#F86E3E` (оранжевый), `#322E2F` (темно-серый)
- Шрифты: `--font-oranienbaum`, `--font-inter`
- Радиусы: `--radius-xs`, `--radius-sm`, `--radius-md`, `--radius-lg`

**Для Битрикса:**
- Переменные уже включены в `main.css`
- При необходимости можно переопределить через CSS

### 4.3. Компоненты стилей

Все компоненты находятся в `src/scss/ui-kit/`:
- `_hero.scss` - блок Hero с формой
- `_sections.scss` - все секции (about, rooms, gallery, etc.)
- `_admin.scss` - админ панель
- `_datepicker.scss` - стили календарей
- `_modal.scss` - модальные окна
- И другие...

---

## 📱 JavaScript функциональность

### 5.1. Подключение скриптов

**Файл**: `src/js/main.js` - весь JavaScript код

**Для Битрикса:**
1. Скопируйте `dist/js/main.js` в шаблон
2. Подключите в `footer.php` перед `</body>`:
   ```php
   <script src="<?= SITE_TEMPLATE_PATH ?>/js/main.js"></script>
   ```

### 5.2. Зависимости

**Библиотеки, которые должны быть подключены ДО main.js:**

```html
<!-- Swiper -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.css">
<script src="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.js"></script>

<!-- Air Datepicker -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/air-datepicker@3.6.0/air-datepicker.min.css">
<script src="https://cdn.jsdelivr.net/npm/air-datepicker@3.6.0/air-datepicker.min.js"></script>

<!-- Fancybox -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0.36/dist/fancybox/fancybox.css">
<script src="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0.36/dist/fancybox/fancybox.umd.js"></script>
```

**Или используйте локальные файлы из `node_modules` после сборки.**

### 5.3. Инициализация

JavaScript автоматически инициализируется при загрузке страницы:
- Календари (Hero форма, модальные окна, админка)
- Слайдеры (Swiper)
- Модальные окна (Fancybox)
- Счетчики (взрослые/дети)
- Фильтры номеров

**Никакой дополнительной инициализации не требуется!**

---

## 🗄️ Структура данных

### 6.1. Номера (Rooms)

**Формат данных в HTML:**
```html
<div class="rooms__card" 
     data-room-id="1" 
     data-room-capacity="4" 
     data-room-category="family"
     data-room-bookings='[{"checkin":"2026-02-15","checkout":"2026-02-20","roomId":"1"}]'>
```

**Для Битрикса:**
- Создайте инфоблок "Номера" с полями:
  - ID номера
  - Вместимость (взрослые)
  - Вместимость (дети)
  - Категория
  - Изображение
  - Цена
  - Описание
- При выводе заполняйте `data-атрибуты` из инфоблока

### 6.2. Бронирования

**Формат данных:**
```json
[
  {
    "checkin": "2026-02-15",
    "checkout": "2026-02-20",
    "roomId": "1",
    "id": "431",
    "guest": "Имя",
    "city": "Город",
    "guests": "3",
    "price": "45000",
    "comment": "Комментарий"
  }
]
```

**Для Битрикса:**
- Создайте инфоблок "Бронирования"
- При выводе календарей передавайте данные через `data-room-bookings`

---

## 🔐 Админ панель

### 7.1. Страница входа

**Файл**: `src/admin-login.html`

**Для Битрикса:**
- Используйте стандартную авторизацию Битрикса
- Стилизуйте форму входа по дизайну из `admin-login.html`

### 7.2. Админ панель

**Файл**: `src/admin.html`

**Функциональность:**
- Создание/редактирование бронирований
- Просмотр календарей по номерам
- Таблица всех бронирований
- Подсчет общей суммы

**Для Битрикса:**
- Создайте раздел в админке Битрикса
- Используйте компоненты для работы с инфоблоками
- Календари можно оставить как есть (они работают на клиенте)

---

## 📝 Важные замечания

### 8.1. Календари

- Все календари используют библиотеку **Air Datepicker**
- Стили календарей находятся в `src/scss/ui-kit/_datepicker.scss`
- Кнопка "Очистить" отключена во всех календарях (`buttons: []`)

### 8.2. Модальные окна

- Используют **Fancybox 5**
- Загружаются динамически через `data-fancybox-dialog`
- Не требуют предварительной загрузки в DOM

### 8.3. Слайдеры

- Используют **Swiper 12**
- Автоматически инициализируются для:
  - Галереи
  - Табов номеров (на мобильных)
  - Преимуществ
  - Достопримечательностей

### 8.4. Адаптивность

- Проект полностью адаптивен
- Брейкпоинты: 480px, 768px, 992px, 1024px, 1200px
- На мобильных некоторые элементы скрываются или меняют структуру

---

## 🚀 Пошаговая интеграция

### Шаг 1: Подготовка файлов
1. Выполните `npm run build`
2. Скопируйте файлы из `dist/` в шаблон Битрикса

### Шаг 2: Создание шаблона
1. Создайте новый шаблон в Битриксе
2. Скопируйте `header.html` → `header.php`
3. Скопируйте `footer.html` → `footer.php`
4. Подключите CSS и JS файлы

### Шаг 3: Интеграция данных
1. Создайте инфоблоки для номеров и бронирований
2. Замените статические данные на компоненты Битрикса
3. Настройте передачу данных бронирований в `data-атрибуты`

### Шаг 4: Настройка форм
1. Создайте веб-формы для лидов и бронирований
2. Настройте обработчики на сохранение в инфоблоки
3. Подключите отправку email-уведомлений

### Шаг 5: Тестирование
1. Проверьте работу всех форм
2. Проверьте календари и блокировку забронированных дат
3. Проверьте модальные окна
4. Проверьте адаптивность на разных устройствах

---

## 📞 Контакты для вопросов

При возникновении вопросов по интеграции обращайтесь к разработчику фронтенда.

---

## 📌 Чеклист интеграции

- [ ] Файлы скопированы в шаблон Битрикса
- [ ] CSS и JS подключены
- [ ] Header и Footer интегрированы
- [ ] Инфоблоки созданы
- [ ] Данные номеров выводятся с правильными data-атрибутами
- [ ] Формы настроены и работают
- [ ] Календари блокируют забронированные даты
- [ ] Модальные окна открываются корректно
- [ ] Адаптивность проверена
- [ ] Все изображения загружаются

---

**Версия документа**: 1.0  
**Дата создания**: 2026-01-27
